// nftService.ts - Re-export from JavaScript version
// This avoids the Vite React plugin preamble detection error

import { nftService } from './nftServiceJs';

export { nftService };
export default nftService;
