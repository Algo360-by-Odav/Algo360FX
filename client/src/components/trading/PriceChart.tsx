// @ts-nocheck
// This is a minimal version of PriceChart.tsx that doesn't use JSX
// It simply re-exports from the JavaScript version to avoid Vite React plugin errors

// Import from the JavaScript version
import PriceChart from './priceChartJs.js';

// Re-export the component
export { PriceChart };
