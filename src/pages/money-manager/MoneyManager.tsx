import React from 'react';
import { Box, Typography } from '@mui/material';

const MoneyManager: React.FC = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Money Manager</Typography>
      {/* Add money manager content here */}
    </Box>
  );
};

export default MoneyManager;
